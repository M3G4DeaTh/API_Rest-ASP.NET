# API_Rest-ASP.NET
 Simple rest api
C# program, it runs in localhost with swagger, u can do http requests, like put, get, post.
Its a simple example, we have models for data, and we storage that data in a local database (SQLITE).
